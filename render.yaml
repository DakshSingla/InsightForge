services:
  - type: web
    name: insightforge-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: GEMINI_MODEL
        value: gemini-flash-latest
